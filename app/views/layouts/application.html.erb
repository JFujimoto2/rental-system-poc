<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "賃貸管理システム" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="賃貸管理システム">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <header class="site-header">
        <div class="header-inner">
          <div class="header-left">
            <%= link_to "賃貸管理システム", root_path, class: "site-title" %>
          </div>
          <nav class="main-nav">
            <div class="nav-group">
              <button type="button" class="nav-group-label">物件管理</button>
              <ul class="nav-dropdown">
                <li><%= link_to "建物一覧", buildings_path %></li>
                <li><%= link_to "部屋一覧", rooms_path %></li>
              </ul>
            </div>
            <div class="nav-group">
              <button type="button" class="nav-group-label">契約管理</button>
              <ul class="nav-dropdown">
                <li><%= link_to "オーナー一覧", owners_path %></li>
                <li><%= link_to "マスターリース契約", master_leases_path %></li>
                <li class="nav-divider"></li>
                <li><%= link_to "入居者一覧", tenants_path %></li>
                <li><%= link_to "転貸借契約", contracts_path %></li>
              </ul>
            </div>
            <div class="nav-group">
              <button type="button" class="nav-group-label">入出金管理</button>
              <ul class="nav-dropdown">
                <li><%= link_to "テナント入金", tenant_payments_path %></li>
                <li><%= link_to "オーナー支払", owner_payments_path %></li>
              </ul>
            </div>
            <div class="nav-group">
              <button type="button" class="nav-group-label">データ管理</button>
              <ul class="nav-dropdown">
                <li><%= link_to "インポート", new_import_path %></li>
                <% if current_user.can_manage_users? %>
                  <li class="nav-divider"></li>
                  <li><%= link_to "ユーザー管理", users_path %></li>
                <% end %>
              </ul>
            </div>
          </nav>
          <div class="header-right">
            <div class="user-info">
              <span class="user-name"><%= current_user.name %></span>
              <span class="user-role"><%= current_user.role_label %></span>
            </div>
            <%= button_to "ログアウト", logout_path, method: :delete, class: "btn-logout" %>
          </div>
        </div>
      </header>
    <% end %>

    <% if logged_in? %>
      <main class="container">
        <% if notice.present? %>
          <div class="flash flash-notice"><%= notice %></div>
        <% end %>
        <% if alert.present? %>
          <div class="flash flash-alert"><%= alert %></div>
        <% end %>
        <%= yield %>
      </main>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
