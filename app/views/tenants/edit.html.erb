<% content_for :title, "入居者編集" %>

<h1>入居者編集</h1>

<%= render "form", tenant: @tenant %>

<div class="actions-bar">
  <%= link_to "詳細に戻る", @tenant, class: "btn" %>
  <%= link_to "一覧に戻る", tenants_path, class: "btn" %>
</div>
